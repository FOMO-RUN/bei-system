v0.1.0 中文化措施总结
一、目录结构中文化
原值	新值	配置方式
crates/	包/	直接重命名
src/	源码/	Cargo.toml path 配置
target/	构建/	
.cargo/config.toml
 + MSVC 工具链
docs/	文档/	直接重命名
runs/	
回放/
直接重命名
skills/	
技能/
直接重命名
二、文件命名中文化
原值	新值	配置方式
main.rs	入口.rs	[[bin]] path
lib.rs	库.rs	[lib] path
events.jsonl
事件流.jsonl	代码中硬编码
artifacts/	产物/	代码中硬编码
bei.exe
悖.exe	[[bin]] name = "悖"
三、宏封装（包/宏/源码/库.rs）
rust
换行打印!()  // → println!
打印!()      // → print!
格式化!()    // → format!
向量![]      // → vec![]
断言!()      // → assert!
有!(值)      // → Some(值)
成!(值)      // → Ok(值)
败!(错误)    // → Err(错误)
四、扩展方法（包/宏/源码/扩展.rs）
rust
option.解包()      // → .unwrap()
vec.推入(x)        // → .push(x)
iter.过滤(f)       // → .filter(f)
iter.收集()        // → .collect()
iter.映射(f)       // → .map(f)
str.长度()         // → .len()
五、工具链选择
工具链	Unicode 路径支持	选择
GNU (mingw)	❌ 不支持	已移除
MSVC	✅ 支持	采用
六、智能检查工具
白名单：跳过宏定义包和检查器本身
安全替换：只检查可安全替代的项目
跳过语法限制：Some/None/Ok 在模式匹配中无法替代
七、无法中文化的项目
项目	原因
crate 名称 (bei_core)	Cargo 仅允许 ASCII
README.md
GitHub 需要
Cargo.toml
Cargo 约定
模式匹配 (Some(x) =>)	Rust 语法限制
最终效果：检查工具显示 100% 中文化覆盖率（在可控范围内）